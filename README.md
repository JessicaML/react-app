# react-pouchdb

simple app showing how to create db and document in pouch, and render to the page with react. (In progress!)

git clone git@github.com:JessicaML/react-pouchdb.git

cd react-pouchdb

npm install

npm run build

npm start
